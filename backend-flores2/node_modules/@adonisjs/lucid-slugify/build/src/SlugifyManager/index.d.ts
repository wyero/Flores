/// <reference path="../../adonis-typings/index.d.ts" />
/// <reference types="@adonisjs/application/build/adonis-typings" />
import { ApplicationContract } from '@ioc:Adonis/Core/Application';
import { SlugifyConfig, StrategiesList, ExtendCallback, SlugifyManagerContract } from '@ioc:Adonis/Addons/LucidSlugify';
/**
 * Slugify manager manages the lifecycle of strategies
 */
export declare class SlugifyManager implements SlugifyManagerContract {
    application: ApplicationContract;
    /**
     * A private map of strategies added from outside in.
     */
    private extendedStrategies;
    constructor(application: ApplicationContract);
    /**
     * Makes an instance of the simple strategy
     */
    private makeSimpleStrategy;
    /**
     * Makes an instance of the dbIncrement strategy
     */
    private makeDbIncrementStrategy;
    /**
     * Makes an instance of the shortid strategy
     */
    private makeShortIdStrategy;
    /**
     * Make extended strategy instance
     */
    private makeExtendedStrategy;
    /**
     * Makes an instance of the given strategy
     */
    use(strategy: keyof StrategiesList, config: SlugifyConfig): any;
    /**
     * Extend by adding custom strategies
     */
    extend(strategy: string, callback: ExtendCallback): void;
}
