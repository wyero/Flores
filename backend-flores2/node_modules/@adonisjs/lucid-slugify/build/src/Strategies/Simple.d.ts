/// <reference path="../../adonis-typings/index.d.ts" />
/// <reference types="@adonisjs/lucid" />
import { LucidModel } from '@ioc:Adonis/Lucid/Orm';
import { SlugifyConfig, SlugifyStrategyContract } from '@ioc:Adonis/Addons/LucidSlugify';
/**
 * A simple strategy to generate slugs
 */
export declare class SimpleStrategy implements SlugifyStrategyContract {
    private config;
    protected separator: string;
    protected maxLengthBuffer: number;
    constructor(config: SlugifyConfig);
    /**
     * Makes the slug out the value string
     */
    makeSlug(_: LucidModel, __: string, value: string): string;
    /**
     * Returns the slug as it is
     */
    makeSlugUnique(_: LucidModel, __: string, slug: string): Promise<string>;
}
