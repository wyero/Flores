/// <reference path="../../adonis-typings/index.d.ts" />
/// <reference types="@adonisjs/lucid" />
import { LucidModel } from '@ioc:Adonis/Lucid/Orm';
import { SlugifyStrategyContract } from '@ioc:Adonis/Addons/LucidSlugify';
import { SimpleStrategy } from './Simple';
/**
 * A Short id strategy that appends a shortid to the base slug
 */
export declare class ShortIdStrategy extends SimpleStrategy implements SlugifyStrategyContract {
    protected maxLengthBuffer: number;
    /**
     * Add shortid to the slug
     */
    makeSlugUnique(_: LucidModel, __: string, slug: string): Promise<string>;
}
