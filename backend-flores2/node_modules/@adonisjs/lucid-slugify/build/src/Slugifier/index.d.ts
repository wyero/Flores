/// <reference path="../../adonis-typings/index.d.ts" />
/// <reference types="@adonisjs/lucid" />
import { LucidModel, LucidRow } from '@ioc:Adonis/Lucid/Orm';
import { SlugifyConfig, SlugifyStrategyContract } from '@ioc:Adonis/Addons/LucidSlugify';
/**
 * Makes the slug for a given model and field
 */
export declare class Slugifier {
    private strategy;
    private model;
    private field;
    private config;
    private separator;
    constructor(strategy: SlugifyStrategyContract, model: LucidModel, field: string, config: SlugifyConfig);
    /**
     * Transform a given value to a string
     */
    private transformValueToSlugString;
    /**
     * Returns the slug value from the configured fields
     */
    private getSlugValue;
    /**
     * Makes a slug for a given instance of model
     */
    makeSlug(row: LucidRow): Promise<string | null>;
}
